import { UploadFootageDialog } from "@/components/UploadFootageDialog";
import { LibrarySourceStatus } from "@/components/LibrarySourceStatus";
import { SketchShell } from "@/components/SketchShell";
import { useFootageSelection } from "@/contexts/FootageSelectionContext";
import { demoImages, formatDuration, gradients, hideBrokenImageElement, matchReasons, type Clip } from "@/lib/footage";
import { getScopedLibrarySource } from "@/lib/librarySource";
import { cn } from "@/lib/utils";
import { trpc } from "@/lib/trpc";
import { Archive, Check, ChevronRight, MoreHorizontal, Search, Sparkles, WandSparkles, X } from "lucide-react";
import { useMemo, useState } from "react";
import { toast } from "sonner";
import { Link } from "wouter";

const filters = ["All clips", "Night", "Warm", "Quiet", "People", "Wide", "Moving"];
type SimilarDimension = "all" | "color" | "mood" | "lighting" | "subject" | "composition" | "motion";

export default function Library() {
  const [searchQuery, setSearchQuery] = useState("");
  const [activeFilter, setActiveFilter] = useState("All clips");
  const [focusedClipId, setFocusedClipId] = useState<number | null>(104);
  const [showSimilar, setShowSimilar] = useState(false);
  const [similarDimension, setSimilarDimension] = useState<SimilarDimension>("all");
  const [uploadOpen, setUploadOpen] = useState(false);
  const { selectedIds, toggleSelection, isSelected } = useFootageSelection();
  const searchInput = useMemo(() => ({ query: searchQuery.trim() }), [searchQuery]);
  const similarInput = useMemo(() => ({ clipId: focusedClipId ?? 0, dimension: similarDimension }), [focusedClipId, similarDimension]);
  const library = trpc.footage.sampleList.useQuery();
  const searched = trpc.footage.sampleSearch.useQuery(searchInput, { enabled: searchQuery.trim().length > 1 });
  const similar = trpc.footage.sampleSimilar.useQuery(similarInput, { enabled: Boolean(focusedClipId && showSimilar) });
  const sampleCollections = trpc.collections.sampleList.useQuery();
  const sampleSource = getScopedLibrarySource(library.data, "sample");
  const baseClips = sampleSource.clips as Clip[];
  const derivedClips = showSimilar ? (similar.data?.clips ?? []) as Clip[] : searchQuery.trim().length > 1 ? (searched.data?.clips ?? []) as Clip[] : baseClips;
  const filterTerm = activeFilter === "All clips" ? "" : activeFilter;
  const visibleClips = filterTerm ? derivedClips.filter(clip => [clip.description, ...clip.subjects, ...clip.mood, ...clip.colors, clip.shotType, clip.cameraMotion].join(" ").toLowerCase().includes(filterTerm.toLowerCase())) : derivedClips;
  const focusedClip = baseClips.find(clip => clip.id === focusedClipId) ?? visibleClips[0] ?? null;
  return <><SketchShell active="library" onUpload={() => setUploadOpen(true)}><div className="mx-auto max-w-[1560px] px-4 pb-12 pt-8 sm:px-7 lg:px-10"><section className="grid gap-7 xl:grid-cols-[minmax(0,1fr)_300px]"><div><div className="mb-5 rounded-2xl border-[1.5px] border-[#2c2922]/55 bg-[#fff1ba] p-4 shadow-[2px_2px_0_rgba(44,41,34,.13)]"><div className="flex flex-wrap items-start justify-between gap-3"><div><LibrarySourceStatus mode="sample" /><h1 className="mt-1 font-hand text-4xl font-bold leading-none">Try the product before making it yours.</h1><p className="mt-2 max-w-2xl text-xs leading-5 ink-muted">These eight fictional clips demonstrate search, visual notes, selection, and creative reasoning. They are not your uploads, and no action here changes your personal workspace.</p></div><Link href="/docs#sample" className="inline-flex shrink-0 items-center gap-1 rounded-lg border border-[#2c2922]/40 bg-[#fffdf7] px-2.5 py-1.5 text-[11px] font-semibold shadow-sm">What is sample mode? <ChevronRight className="size-3" /></Link></div></div><div className="relative max-w-2xl"><Search className="absolute left-4 top-1/2 size-4 -translate-y-1/2 ink-muted" /><input value={searchQuery} onChange={event => { setSearchQuery(event.target.value); setShowSimilar(false); }} placeholder="Search a memory — “quiet blue night shots”" className="h-11 w-full rounded-xl border-[1.5px] border-[#2c2922]/55 bg-[#fffdf7] pl-11 pr-10 text-sm shadow-[2px_2px_0_rgba(44,41,34,.1)] outline-none placeholder:text-[#756c60] focus:ring-2 focus:ring-[#e69275]" /></div><div className="mt-5 flex items-center gap-2 overflow-x-auto pb-1">{filters.map(filter => <button key={filter} onClick={() => setActiveFilter(filter)} className={cn("shrink-0 rounded-full border-[1.5px] px-3.5 py-1.5 text-xs transition-colors", activeFilter === filter ? "border-[#2c2922] bg-[#f8d9cc] font-semibold shadow-[1px_1px_0_#2c2922]" : "border-[#2c2922]/35 bg-[#fffdf7]/75 ink-muted hover:bg-[#e8eff7] hover:text-[#2c2922]")}>{filter}</button>)}{(searchQuery || showSimilar) && <button onClick={() => { setSearchQuery(""); setShowSimilar(false); setActiveFilter("All clips"); }} className="ml-1 flex shrink-0 items-center gap-1 text-xs ink-muted hover:text-[#2c2922]"><X className="size-3" />Clear</button>}</div>{showSimilar && <div className="note-blue mt-5 flex flex-wrap items-center gap-2 rounded-xl border-[1.5px] border-[#2c2922]/48 px-3 py-2.5 shadow-[2px_2px_0_rgba(44,41,34,.12)]"><WandSparkles className="size-3.5" /><span className="mr-1 text-xs">Connections to <b>{focusedClip?.fileName}</b></span>{(["all", "color", "lighting", "mood", "composition", "motion"] as const).map(dimension => <button key={dimension} onClick={() => setSimilarDimension(dimension)} className={cn("rounded-md px-2 py-1 text-[11px] capitalize", similarDimension === dimension ? "bg-white/80 font-semibold shadow-[1px_1px_0_rgba(44,41,34,.2)]" : "ink-muted hover:text-[#2c2922]")}>{dimension}</button>)}</div>}{!showSimilar && searchQuery.trim().length > 1 && <div className="note-green mt-5 flex items-center gap-2 rounded-xl border-[1.5px] border-[#2c2922]/48 px-3 py-2.5 text-xs shadow-[2px_2px_0_rgba(44,41,34,.12)]"><Search className="size-3.5 shrink-0" /><span><b>{visibleClips.length} matches</b> for “{searchQuery.trim()}”. Each card shows its matching clues.</span></div>}<div className="mt-6 grid gap-4 min-[520px]:grid-cols-2 2xl:grid-cols-3">{library.isLoading ? Array.from({ length: 6 }).map((_, index) => <div key={index} className="h-[252px] animate-pulse rounded-2xl border border-[#2c2922]/30 bg-[#fffdf7]/80" />) : visibleClips.map((clip, index) => <ClipCard key={clip.id} clip={clip} index={index} selected={isSelected(clip.id)} focused={focusedClip?.id === clip.id} reasons={showSimilar ? [] : matchReasons(clip, searchQuery)} onFocus={() => { setFocusedClipId(clip.id); setShowSimilar(false); }} onToggle={() => toggleSelection(clip.id)} />)}</div>{!visibleClips.length && <div className="paper-panel mt-4 rounded-2xl px-6 py-14 text-center"><Archive className="mx-auto size-6 ink-muted" /><p className="mt-3 text-sm font-semibold">Nothing surfaced just yet.</p><p className="mt-1 text-xs ink-muted">Try a feeling, colour, subject, time of day, or a simpler phrase.</p></div>}</div><aside className="space-y-5 xl:pt-2">{focusedClip && <div className="tape note-yellow rounded-2xl border-[1.5px] border-[#2c2922]/58 p-4 shadow-[3px_3px_0_rgba(44,41,34,.14)]"><div className={cn("relative aspect-[16/10] overflow-hidden rounded-xl border-[1.5px] border-[#2c2922]/55 bg-gradient-to-br", gradients[Math.abs(focusedClip.id) % gradients.length])}>{(focusedClip.thumbnailUrl ?? demoImages[focusedClip.id]) && <img src={focusedClip.thumbnailUrl ?? demoImages[focusedClip.id]} alt="" onError={event => hideBrokenImageElement(event.currentTarget)} className="h-full w-full object-cover" />}<div className="absolute inset-x-3 bottom-3 flex items-center justify-between"><span className="rounded-md bg-[#fffdf7]/90 px-1.5 py-1 font-mono text-[9px] shadow-sm">{formatDuration(focusedClip.durationMs)}</span><button onClick={() => setShowSimilar(true)} className="rounded-md border border-[#2c2922]/55 bg-[#fffdf7]/90 px-2 py-1 text-[10px] font-semibold shadow-sm hover:bg-white">Find similar</button></div></div><div className="mt-4 flex items-start justify-between gap-2"><div><p className="font-mono text-[10px] ink-muted">CLIP NOTES · SAMPLE</p><p className="mt-1 text-sm font-bold">{focusedClip.fileName}</p></div><button onClick={() => toggleSelection(focusedClip.id)} className={cn("grid size-7 place-items-center rounded-lg border-[1.5px]", isSelected(focusedClip.id) ? "border-[#2c2922] bg-[#f4ad89]" : "border-[#2c2922]/45 bg-[#fffdf7] ink-muted hover:text-[#2c2922]")}><Check className="size-3.5" /></button></div><p className="mt-3 text-xs leading-5 ink-muted">{focusedClip.description}</p><div className="mt-4 flex flex-wrap gap-1.5">{[...focusedClip.mood, focusedClip.shotType, ...focusedClip.colors.slice(0, 1)].map(tag => <span key={tag} className="rounded-md border border-[#2c2922]/25 bg-[#fffdf7]/65 px-1.5 py-1 text-[10px]">{tag}</span>)}</div></div>}<div className="paper-panel rounded-2xl p-4"><p className="font-hand text-xl font-bold">Sample selection</p><p className="mt-1 text-xs leading-5 ink-muted">Circle sample clips to try the selection and Ask My Footage flow. Nothing is saved to your Workspace.</p><div className="mt-4 flex items-center justify-between"><span className="rounded-md bg-[#dcefdc] px-2 py-1 font-mono text-[10px]">{selectedIds.length} circled</span><Link href="/ask" className="inline-flex items-center gap-1 text-xs font-bold underline decoration-wavy decoration-[#e69275] underline-offset-4">Ask about sample <Sparkles className="size-3" /></Link></div></div><div className="note-pink rounded-2xl border-[1.5px] border-[#2c2922]/48 p-4 shadow-[2px_2px_0_rgba(44,41,34,.12)]"><p className="font-hand text-xl font-bold">Sample collections</p><p className="mt-1 text-xs leading-5 ink-muted">Fictional groupings made only from the demo clips.</p><div className="mt-3 space-y-2">{(sampleCollections.data?.collections ?? []).map(collection => <button key={collection.id} onClick={() => toast.info(`${collection.name}: ${collection.description}`)} className="flex w-full items-center justify-between rounded-lg border border-[#2c2922]/25 bg-[#fffdf7]/75 px-2.5 py-2 text-left text-[11px] font-semibold"><span className="truncate">{collection.name}</span><span className="ml-2 shrink-0 text-[10px] ink-muted">{collection.clipCount} clips</span></button>)}</div></div></aside></section></div></SketchShell><UploadFootageDialog open={uploadOpen} onOpenChange={setUploadOpen} /></>;
}

function ClipCard({ clip, index, selected, focused, reasons, onFocus, onToggle }: { clip: Clip; index: number; selected: boolean; focused: boolean; reasons: string[]; onFocus: () => void; onToggle: () => void }) {
  const imageUrl = clip.thumbnailUrl ?? demoImages[clip.id];
  return <article onClick={onFocus} className={cn("sketch-card group overflow-hidden rounded-2xl border-[1.5px] bg-[#fffdf7]", focused ? "border-[#2c2922]" : "border-[#2c2922]/50")}><div className={cn("relative aspect-[16/10] overflow-hidden border-b-[1.5px] border-[#2c2922]/45 bg-gradient-to-br", gradients[index % gradients.length])}>{imageUrl && <img src={imageUrl} alt="" onError={event => hideBrokenImageElement(event.currentTarget)} className="h-full w-full object-cover" />}<div className="absolute inset-x-3 top-3 flex items-center justify-between"><button onClick={event => { event.stopPropagation(); onToggle(); }} className={cn("grid size-6 place-items-center rounded-full border-[1.5px] shadow-sm transition-colors", selected ? "border-[#2c2922] bg-[#f4ad89]" : "border-[#2c2922]/50 bg-[#fffdf7]/90 hover:bg-[#dcefdc]")}><Check className="size-3.5" /></button><div className="flex items-center gap-1.5"><span className="rounded-md bg-[#fffdf7]/90 px-1.5 py-1 font-mono text-[9px] shadow-sm">{formatDuration(clip.durationMs)}</span><button onClick={event => { event.stopPropagation(); toast.info("Pick a clip to see its notes or find similar material."); }} className="grid size-6 place-items-center rounded-md border border-[#2c2922]/35 bg-[#fffdf7]/90 opacity-0 shadow-sm transition-opacity group-hover:opacity-100"><MoreHorizontal className="size-3.5" /></button></div></div></div><div className="p-3.5"><div className="flex items-center justify-between gap-2"><p className="truncate font-mono text-[10px] ink-muted">{clip.fileName}</p><span className="shrink-0 rotate-1 rounded bg-[#e8eff7] px-1 text-[9px] uppercase tracking-[.12em] ink-muted">{clip.time}</span></div><p className="mt-2 line-clamp-2 text-xs leading-5 text-[#37332c]">{clip.description}</p><div className="mt-3 flex flex-wrap gap-1.5">{[clip.mood[0], clip.shotType].filter(Boolean).map(tag => <span key={tag} className="rounded-md border border-[#2c2922]/25 bg-[#f4f0e7] px-1.5 py-1 text-[10px]">{tag}</span>)}</div>{reasons.length > 0 && <p className="mt-2 text-[10px] text-[#4d7969]">Matches: {reasons.join(" · ")}</p>}</div></article>;
}
